stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  script:
    - docker build -t app-ads-worker .

test-job:
  stage: test
  script:
    - docker run --rm app-ads-worker npm test

deploy-job:
  stage: deploy
  script:
    - docker compose up -d
